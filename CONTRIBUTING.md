# Contributing

## Workflow

1. Create a branch from `main`.
2. Keep commits focused and reviewable.
3. Run local checks before opening a PR.
4. Use the PR template and include validation steps.

## Local Validation

```powershell
.\gradlew.bat test
.\gradlew.bat lint
```
